@model IEnumerable<PC_FORUM.Models.Category>

<h2>Категории</h2>
<ul>
    @foreach (var category in Model)
    {
        <li>
            <a asp-controller="Topic" asp-action="Index" asp-route-id="@category.Id">@category.Name</a>
        </li>
    }
</ul>
